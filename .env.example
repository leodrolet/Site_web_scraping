# Clé secrète : signe les sessions ET chiffre les clés API des utilisateurs.
# Génère une vraie valeur aléatoire avec :
#   python -c "import secrets; print(secrets.token_hex(32))"
# puis colle le résultat ci-dessous (sans espaces ni guillemets).
SECRET_KEY=change_moi_avec_une_vraie_valeur_aleatoire

# NOTE : les clés API Hunter.io / Apollo.io / SerpAPI ne sont PLUS ici.
# Chaque utilisateur saisit les siennes dans l'interface /parametres.
